// CPU information
// current user information
// operating systems' platiform
// system free memory
const os = require("os");
console.log(os.hostname());
console.log(os.freemem());
console.log(os.platform());
console.log(os.arch());
